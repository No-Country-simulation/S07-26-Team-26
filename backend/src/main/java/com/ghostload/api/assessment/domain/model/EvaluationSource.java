package com.ghostload.api.assessment.domain.model;

// De dónde vino el operador cuando se registró: directo desde la calculadora,
// desde el benchmark, o porque llegó por una invitación de outreach.
public enum EvaluationSource {
    CALCULATOR,
    BENCHMARK,
    OUTREACH
}
